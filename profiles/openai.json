{
  "routes": {
    "openai": {
      "upstream": "https://api.openai.com",
      "inject_mode": "header",
      "credential_sources": [
        {
          "env": "OPENAI_API_KEY",
          "inject_header": "Authorization",
          "format": "Bearer {}"
        }
      ]
    }
  }
}